pipeline {
   agent { docker { image 'python:latest'}}
   
   stage('Initialize'){
        def dockerHome = tool 'myDocker'
        env.PATH = "${dockerHome}/bin:${env.PATH}"
    }

   stages {

      stage('hello') {
        steps{      
          echo 'Hello!'
        }
        }
        
              stage('version') {
        steps{      
          sh 'python --version'
        }
        }


  }
}
