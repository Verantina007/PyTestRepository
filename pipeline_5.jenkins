pipeline {
   agent { docker { image 'python:latest'}}

   stages {

      stage('runpy') {
        steps{      
          sh 'python tests.py'
        }
        }
		
	  stage('version') {
        steps{      
          sh 'python --version'
        }
        }

  }
}